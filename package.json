{
  "name": "@kontist/mock-solaris",
  "version": "0.0.0",
  "description": "mocksolaris server",
  "main": "dist/index.js",
  "types": "dist/index.d.js",
  "scripts": {
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "transpile": "tsc && babel --root-mode upward src --extensions \".js\" --out-dir dist --copy-files --source-maps --quiet && echo \"--ALL FILES TRANSPILED!\"",
    "transpile-watch": "tsc --watch & babel --root-mode upward src --extensions \".js\" --out-dir dist --copy-files --source-maps --watch"
  },
  "author": "",
  "license": "SEE LICENSE IN PROPERIETARY_LICENSE",
  "dependencies": {
    "bluebird": "^3.4.7",
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "iban-generator": "^0.0.1",
    "lodash": "^4.17.11",
    "moment": "^2.17.1",
    "node-fetch": "^2.3.0",
    "node-uuid": "^1.4.7",
    "redis": "^2.6.5",
    "swig": "^1.4.2"
  },
  "devDependencies": {
    "nodemon": "^1.18.11"
  }
}
