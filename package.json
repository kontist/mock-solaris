{
  "name": "@kontist/mock-solaris",
  "version": "0.0.0",
  "description": "mocksolaris server",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint --max-warnings 0 ./src && flow status",
    "lint-fix": "eslint ./src --fix",
    "transpile": "babel --root-mode upward src --extensions \".js\" --out-dir dist --copy-files --source-maps --quiet && echo \"--ALL FILES TRANSPILED!\"",
    "transpile-watch": "babel --root-mode upward src --extensions \".js\" --out-dir dist --copy-files --source-maps --watch"
  },
  "author": "",
  "license": "SEE LICENSE IN PROPERIETARY_LICENSE",
  "dependencies": {
    "bluebird": "^3.4.7",
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "express-winston": "^2.0.0",
    "iban-generator": "^0.0.1",
    "lodash": "^4.17.11",
    "moment": "^2.17.1",
    "node-fetch": "^2.3.0",
    "node-uuid": "^1.4.7",
    "redis": "^2.6.5",
    "redis-mock": "^0.44.0",
    "swig": "^1.4.2",
    "winston": "^2.4.4",
    "winston-loggly-bulk": "2.0.3"
  },
  "devDependencies": {
    "nodemon": "^1.18.11"
  },
  "resolutions": {
    "swig/uglify-js": "2.8.29"
  }
}
